<!DOCTYPE html>
<html>
  <head>
    <title>Movie Snob</title>
  </head>
  <body>
    <h1>Movie Snob</h1>
    <%# shows recent posts about movies
    allows user to login/log out
    but doesn't allow a user to comment or edit post %>
    <div class="card-columns">
      <% Movie.all.each do |movie|%>
      <div class="card">
        <img class="card-img-top" src="<%= movie.poster %>" alt="movie.blurb">
        <div class="card-body">
          <a href = "/movies/<%=movie.id%>"><h5 class="card-title"><%= movie.title %></h3></a>
          <p class="card-text"><%= movie.blurb %></p>
          <p class="card-text"><small class="text-muted"><%= movie.rating %></small></p>
        </div>
      </div>
      <% end %>
    </div>
  </body>
</html>